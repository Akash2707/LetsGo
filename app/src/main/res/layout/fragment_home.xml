<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.sample.carpool.carpool.activities.LoginActivity">

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map_offer_ride"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.sample.carpool.carpool.activities.MapsActivity" />

    <Button
        android:id="@+id/offer_ride_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/margin_top_btn"
        android:layout_marginStart="@dimen/margin_top_btn"
        android:background="@drawable/custom_btn"
        android:text="Offer Ride"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

    <Button
        android:id="@+id/get_ride_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/margin_top_btn"
        android:layout_marginEnd="@dimen/margin_top_btn"
        android:background="@drawable/custom_btn"
        android:text="Get Ride"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

</RelativeLayout>

